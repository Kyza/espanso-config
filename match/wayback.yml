matches:
   - trigger: "wayback;;"
     label: Wayback Machine
     replace: "{{output}}"
     vars:
        - name: "form"
          type: "form"
          params:
             layout: |
                [[url]]
                [[target]]
                [[action]]
             fields:
                url:
                   type: text
                target:
                   type: list
                   default: Latest
                   values: |
                      Latest
                      Overview
                      Save
                action:
                   type: list
                   default: Open
                   values: |
                      Open
                      Output
        - name: output
          type: script
          params:
             args:
                - deno
                - --quiet
                - run
                - --allow-run
                - "{{kyzaconfig}}/scripts/wayback.ts"
                - "--target={{form.target}}"
                - "--url={{form.url}}"
                - "--action={{form.action}}"
